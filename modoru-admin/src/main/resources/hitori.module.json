{
  "key": "modoru:admin",
  "version": "${version}",
  "main": "modoru.admin.AdminModule",
  "packages": [
    "modoru.admin"
  ],

  "depends": {
    "java": ">=${java_version}",
    "hitori": ">=${hitori_version}",
    "hitori:resourcepack": {
      "type": "hard",
      "version": ">=${resourcepack_version}"
    },
    "modoru:main": {
      "type": "hard",
      "version": ">=${main_version}"
    }
  }
}