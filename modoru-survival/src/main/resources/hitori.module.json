{
  "key": "modoru:survival",
  "version": "${version}",
  "main": "modoru.survival.SurvivalModule",
  "packages": [
    "modoru.survival"
  ],

  "depends": {
    "java": ">=${java_version}",
    "hitori": ">=${hitori_version}",
    "hitori:resourcepack": {
      "type": "hard",
      "version": ">=${resourcepack_version}"
    },
    "modoru:main": {
      "type": "hard",
      "version": ">=${main_version}"
    }
  }
}